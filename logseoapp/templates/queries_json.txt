{
    "sEcho": {{sEcho}},
    "iTotalRecords": {{iTotalRecords}},
    "iTotalDisplayRecords": {{iTotalDisplayRecords}},
    "aaData":[
    {% for item in querySet %}
    [

        "<a href='{{ item.phrase_id }}'>{{ item.phrase_id__phrase }}</a>",
        "{{ item.num_ips }}",
        "{{ item.num_pages }}",
        "{{ item.num_engines }}"
    ]
    {% if not forloop.last %}
    ,
    {% endif %}
    {% endfor %}
    ]
}


